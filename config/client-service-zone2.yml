eureka:
  instance:
    metadataMap:
      zone: zone2
  client:
    serviceUrl:
      defaultZone: http://localhost:8762/eureka/
server:  
  port: ${PORT:8082}
management:
  security:
    enabled: false  
custom:
  message: Hello, client-service-zone2!  